<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ตะกร้าสินค้า</title>
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
</head>
<body>
    <%- include('partials/header') %>

    <main class="container mt-5">
        <h1 class="text-center mb-4">ตะกร้าสินค้า</h1>
        <% if (products.length > 0) { %>
            <table class="table">
                <thead>
                    <tr>
                        <th>รูปภาพ</th>
                        <th>ชื่อสินค้า</th>
                        <th>ราคา</th>
                        <th>จำนวน</th>
                        <th>ราคารวม</th>
                    </tr>
                </thead>
                <tbody>
                    <% let total = 0; %>
                    <% products.forEach(product => { %>
                        <tr>
                            <td><img src="<%= product.products_image %>" alt="<%= product.product_name %>" style="max-width: 100px;"></td>
                            <td><%= product.product_name %></td>
                            <td><%= parseFloat(product.price).toFixed(2) %> บาท</td>
                            <td>1</td>
                            <td><%= (parseFloat(product.price) * 1).toFixed(2) %> บาท</td>
                            <% total += parseFloat(product.price) * 1; %>
                        </tr>
                    <% }) %>
                </tbody>
            </table>
            <div class="text-right">
                <h3>ราคารวม: <%= total.toFixed(2) %> บาท</h3>
                <a href="/checkout" class="btn btn-primary">ชำระเงิน</a>
            </div>
        <% } else { %>
            <p>ตะกร้าสินค้าว่างเปล่า</p>
        <% } %>
    </main>

    <%- include('partials/footer') %>
</body>
</html>
