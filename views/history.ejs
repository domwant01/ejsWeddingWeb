<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
</head>
<body>
    <%- include('partials/header') %>

    <main class="container mt-5">
        <h1 class="text-center mb-4">ประวัติการเช่าซื้อ</h1>
        <div class="row">
            <div class="col-md-8 offset-md-2">
                <% if (history.length > 0) { %>
                    <ul class="list-group">
                        <% history.forEach(item => { %>
                            <li class="list-group-item">
                                <strong>วันที่เช่า:</strong> <%= item.rental_date %><br>
                                <strong>สินค้าที่เช่า:</strong> <%= item.product_name %><br>
                                <strong>ราคา:</strong> <%= item.price %> บาท
                            </li>
                        <% }) %>
                    </ul>
                <% } else { %>
                    <p class="text-center">ไม่มีประวัติการเช่าซื้อ</p>
                <% } %>
            </div>
        </div>
    </main>

    <%- include('partials/footer') %>
</body>
</html>
